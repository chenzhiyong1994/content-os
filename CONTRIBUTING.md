# 参与贡献

感谢你愿意改进 Content OS。这个项目更看重可验证的工作流收益，而不是规则数量。

## 开始之前

1. 先读 `AGENTS.md` 与 `docs/operations/workflow-rules.md`。
2. 搜索现有 Issue，确认问题没有正在处理。
3. 涉及流程行为时，说明当前问题、预期行为和可验证证据。

## 本地验证

```powershell
npm test
git diff --check
```

请针对改动面运行最窄且有意义的检查。不要通过跳过测试、削弱断言或隐藏失败让检查通过。

## Pull Request 建议

- 一个 PR 解决一个清楚的问题。
- 描述用户可见变化、兼容性影响和验证结果。
- 不要提交 `workspace/`、真实账号凭证、私人样文、品牌原图或平台导出的原始数据。
- 新增环境变量时同步更新 `.env.example` 和对应运行文档。
- 修改流程、skill 或安全边界时同步更新最近的权威文档。

若改动涉及真实外部发布、付费调用或登录态数据，请提供 dry-run 或 mock 测试，不要在贡献过程中使用他人的账号执行真实动作。
