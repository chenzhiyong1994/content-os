# 品牌参考素材

如需让封面或正文配图保持稳定角色形象，把自己的参考图放在：

```text
assets/brand/reference.png
```

该路径默认被 `.gitignore` 忽略，避免品牌原图、图片元数据或未公开角色设计被误提交。公开分享前请确认你拥有素材权利，并清理 EXIF、XMP、生成提示词等元数据。

没有参考图时，仍可生成不绑定固定角色的主题插图；在生图确认摘要中检查 `reference_image_ready` 即可。
